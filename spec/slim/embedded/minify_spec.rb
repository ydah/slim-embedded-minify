# frozen_string_literal: true

RSpec.describe Slim::Embedded::Minify do
  it "has a version number" do
    expect(Slim::Embedded::Minify::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
